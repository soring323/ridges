def measure(bucket_one: int, bucket_two: int, goal: int, start_bucket: str) -> tuple[int, str, int]:
    # Returns (total_moves, goal_bucket, other_bucket_liters) where goal_bucket is "one" or "two"
    pass
